// import libs
import React from 'react';

// import components
/* import Header from './header/Header';
import BoardChart from './boardchart/BoardChart';
import Shops from './shops/Shops';
import Products from './products/Products';
import Deliveries from './deliveries/Deliveries';
import Customers from './customers/Customers'; */

const DisplayContent = () => {
  return (
    <div className="d-flex flex-column left-menu">
      {/* <Switch>
          <Route path={`${path}/shops`} component={Shops} />
          <Route path={`${path}/deliveries`} component={Deliveries} />
          <Route path={`${path}/products`} component={Products} />
          <Route path={`${path}/customers`} component={Customers} />
        </Switch> */}
      DISPLAY CONTENT
    </div>
  );
};

export default DisplayContent;
